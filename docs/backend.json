{
  "entities": {
    "FarmerProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FarmerProfile",
      "type": "object",
      "description": "Represents a farmer registered with the CropAid Connect system, storing their personal, contact, and agricultural details relevant for subsidy applications and eligibility checks.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the FarmerProfile entity."
        },
        "uniqueFarmerIdentifier": {
          "type": "string",
          "description": "A unique identifier for the farmer, such as a government-issued ID or a system-assigned identifier, used for eligibility checks and tracking. (e.g., Farmer ID)"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the farmer."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the farmer."
        },
        "email": {
          "type": "string",
          "description": "The farmer's email address for communication.",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The farmer's contact phone number, used for eligibility checks and notifications."
        },
        "addressLine1": {
          "type": "string",
          "description": "The first line of the farmer's postal address."
        },
        "addressLine2": {
          "type": "string",
          "description": "The second line of the farmer's postal address (optional)."
        },
        "city": {
          "type": "string",
          "description": "The city where the farmer resides."
        },
        "state": {
          "type": "string",
          "description": "The state or province where the farmer resides."
        },
        "postalCode": {
          "type": "string",
          "description": "The postal code of the farmer's address."
        },
        "country": {
          "type": "string",
          "description": "The country where the farmer resides."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "The farmer's date of birth, used for age-related eligibility criteria.",
          "format": "date"
        },
        "landAreaAcres": {
          "type": "number",
          "description": "The total land area owned or cultivated by the farmer, measured in acres, relevant for many subsidy programs."
        },
        "primaryCropType": {
          "type": "string",
          "description": "The main type of crop cultivated by the farmer, influencing relevant subsidy categories."
        },
        "isRegistered": {
          "type": "boolean",
          "description": "Indicates if the farmer's profile is fully registered and active within the system."
        },
        "registrationDate": {
          "type": "string",
          "description": "The date and time when the farmer's profile was created or first registered in the system.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "uniqueFarmerIdentifier",
        "firstName",
        "lastName",
        "email",
        "phoneNumber",
        "addressLine1",
        "city",
        "state",
        "postalCode",
        "country",
        "dateOfBirth",
        "landAreaAcres",
        "primaryCropType",
        "isRegistered",
        "registrationDate"
      ]
    },
    "SubsidyCategory": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubsidyCategory",
      "type": "object",
      "description": "Defines the different categories of subsidies available through CropAid Connect, including their criteria, benefits, and application details.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SubsidyCategory entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the subsidy category (e.g., 'Fertilizer Subsidy', 'Seed Subsidy', 'Irrigation Subsidy')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the subsidy, its purpose, and goals."
        },
        "eligibilityCriteria": {
          "type": "string",
          "description": "A comprehensive text detailing the requirements farmers must meet to qualify for this subsidy, used by the eligibility checker."
        },
        "benefits": {
          "type": "string",
          "description": "A description of the advantages or support provided to farmers by this subsidy."
        },
        "applicationSteps": {
          "type": "array",
          "description": "A list of ordered steps a farmer needs to follow to successfully apply for this subsidy.",
          "items": {
            "type": "string"
          }
        },
        "iconUrl": {
          "type": "string",
          "description": "The URL to an icon image representing this subsidy category for UI display.",
          "format": "uri"
        },
        "isActive": {
          "type": "boolean",
          "description": "Indicates whether the subsidy category is currently active and accepting new applications."
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "eligibilityCriteria",
        "benefits",
        "applicationSteps",
        "iconUrl",
        "isActive"
      ]
    },
    "Application": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Application",
      "type": "object",
      "description": "Represents a farmer's formal application for a specific subsidy, tracking its status, submission details, and administrative review.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Application entity."
        },
        "farmerProfileId": {
          "type": "string",
          "description": "Reference to the FarmerProfile entity that submitted this application. (Relationship: FarmerProfile 1:N Application)"
        },
        "subsidyCategoryId": {
          "type": "string",
          "description": "Reference to the SubsidyCategory entity that this application is for. (Relationship: SubsidyCategory 1:N Application)"
        },
        "applicationDate": {
          "type": "string",
          "description": "The date and time when the application was initially created or started by the farmer.",
          "format": "date-time"
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time when the application was formally submitted by the farmer for review.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the application (e.g., 'Draft', 'Pending Review', 'Approved', 'Rejected', 'Under Revision')."
        },
        "adminNotes": {
          "type": "string",
          "description": "Any notes added by an administrator during the review process or decision-making."
        },
        "approvalDate": {
          "type": "string",
          "description": "The date and time when the application was officially approved.",
          "format": "date-time"
        },
        "rejectionDate": {
          "type": "string",
          "description": "The date and time when the application was officially rejected.",
          "format": "date-time"
        },
        "rejectionReason": {
          "type": "string",
          "description": "The reason provided by the administrator if the application was rejected."
        }
      },
      "required": [
        "id",
        "farmerProfileId",
        "subsidyCategoryId",
        "applicationDate",
        "status"
      ]
    },
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Stores metadata about documents uploaded by farmers to support their subsidy applications, such as proof of identity or land ownership.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Document entity."
        },
        "applicationId": {
          "type": "string",
          "description": "Reference to the Application entity this document is part of. (Relationship: Application 1:N Document)"
        },
        "farmerProfileId": {
          "type": "string",
          "description": "Reference to the FarmerProfile entity that uploaded this document. (Relationship: FarmerProfile 1:N Document)"
        },
        "documentType": {
          "type": "string",
          "description": "The classification or purpose of the document (e.g., 'Proof of Identity', 'Land Ownership Certificate', 'Bank Statement')."
        },
        "fileName": {
          "type": "string",
          "description": "The original name of the uploaded file provided by the user."
        },
        "storagePath": {
          "type": "string",
          "description": "The URI or path where the document is stored in the application's file storage system.",
          "format": "uri"
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time when the document was uploaded to the system.",
          "format": "date-time"
        },
        "fileExtension": {
          "type": "string",
          "description": "The file extension of the document (e.g., 'pdf', 'jpg', 'png')."
        }
      },
      "required": [
        "id",
        "applicationId",
        "farmerProfileId",
        "documentType",
        "fileName",
        "storagePath",
        "uploadDate",
        "fileExtension"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/farmerProfile",
        "definition": {
          "entityName": "FarmerProfile",
          "schema": {
            "$ref": "#/backend/entities/FarmerProfile"
          },
          "description": "Stores the unique FarmerProfile for an authenticated user. The 'id' field within the FarmerProfile document will match '{userId}', making 'request.auth.uid' the direct owner. This structure provides strong ownership, simplifying security rules. Includes denormalized 'id' matching 'userId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Authentication UID of the farmer."
            }
          ]
        }
      },
      {
        "path": "/subsidyCategories/{subsidyCategoryId}",
        "definition": {
          "entityName": "SubsidyCategory",
          "schema": {
            "$ref": "#/backend/entities/SubsidyCategory"
          },
          "description": "Global collection for all subsidy categories. These documents are publicly readable, providing information on available subsidies, their criteria, and benefits. Writes are restricted to administrators.",
          "params": [
            {
              "name": "subsidyCategoryId",
              "description": "The unique identifier for a specific subsidy category."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/applications/{applicationId}",
        "definition": {
          "entityName": "Application",
          "schema": {
            "$ref": "#/backend/entities/Application"
          },
          "description": "Stores individual subsidy applications submitted by a farmer. The 'farmerProfileId' field will be denormalized to match '{userId}' for robust authorization independence and simplified security rules.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Authentication UID of the farmer submitting the application."
            },
            {
              "name": "applicationId",
              "description": "The unique identifier for a specific application."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Stores metadata for documents uploaded by a farmer, related to their applications. The 'farmerProfileId' field will be denormalized to match '{userId}' for authorization independence and simplified security rules.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Authentication UID of the farmer who uploaded the document."
            },
            {
              "name": "documentId",
              "description": "The unique identifier for a specific uploaded document."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for CropAid Connect prioritizes security, scalability, and debuggability by strictly adhering to the core design principles. Each authenticated user's data is housed within a dedicated `/users/{userId}` sub-tree, leveraging Firebase Authentication's `request.auth.uid` for robust, path-based authorization. This approach naturally makes ownership explicit and simplifies security rules.\n\nAuthorization Independence is achieved through strategic denormalization. For instance, a farmer's `FarmerProfile` is located at `/users/{userId}/farmerProfile`, where the `id` field of the FarmerProfile document explicitly matches the `{userId}` (the `request.auth.uid`). Similarly, `Application` documents are stored in `/users/{userId}/applications/{applicationId}` and `Document` metadata in `/users/{userId}/documents/{documentId}`. In both `Application` and `Document` entities, the `farmerProfileId` field is denormalized to explicitly store the `{userId}`. This eliminates the need for `get()` operations on parent documents in security rules, allowing atomic write operations and preventing complex, hard-to-debug authorization dependencies.\n\nQueryability and Authorization Posture (QAPs) are supported by Structural Segregation. Global, publicly readable data like `SubsidyCategory` information resides in its own top-level collection (`/subsidyCategories`), allowing for simple `allow read: true` rules. All user-specific data (profiles, applications, documents) is nested under `/users/{userId}`, ensuring that `list` operations within these subcollections are implicitly filtered by `request.auth.uid == userId`. This guarantees that users can only list and access their own data, preventing unauthorized enumeration and maintaining QAP compliance. The \"Eligibility Checker\" feature, which involves matching a unique farmer identifier and phone number, is designed to operate securely. While an unauthenticated public lookup on sensitive PII directly within Firestore rules is inherently challenging to secure against enumeration and data leakage, this structure assumes the eligibility check either occurs for an authenticated user against their own `FarmerProfile` (located at `/users/{userId}/farmerProfile`), or through a secure, mediated service (e.g., a Cloud Function) for unauthenticated requests, thereby maintaining the integrity of the Firestore security model. Should a purely Firestore-based unauthenticated lookup be strictly required, a highly segregated and minimal collection with extremely restrictive, field-specific rules (e.g., allowing reads only if specific combined fields match) would be necessary, but this generally introduces complexity and potential security trade-offs that are best avoided when possible. For simplicity and robustness, the primary authorization model for user data relies on `request.auth.uid`.\n\nAdmin roles are managed through a DBAC model using a separate top-level collection (e.g., `/admin_users/{uid}`), where the mere existence of a document signifies admin status. Firestore rules can then check `get(/databases/$(database)/documents/admin_users/$(request.auth.uid)).exists()` for admin-specific write operations on collections like `/subsidyCategories` or to manage `/users/{userId}/applications` across all users."
  }
}